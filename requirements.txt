# Zależności aplikacji — tylko biblioteki używane bezpośrednio.
# Resztę (numpy, pyarrow, altair...) pip dobiera sam w wersjach pasujących
# do Pythona, na którym stoi serwer.
#
# UWAGA — nie przypinaj tutaj numpy ani pandas do starych wersji.
# Streamlit Cloud uruchamia nowszego Pythona, dla którego numpy 1.26.4
# i pandas 2.2.0 nie mają gotowych pakietów. Instalacja całego pliku wtedy
# pada, a Cloud po cichu wraca do własnego środowiska — stąd brała się
# inna wersja Streamlita niż przypięta i inne nazwy atrybutów w HTML.

streamlit>=1.32,<2
pandas>=2.2,<3
openpyxl>=3.1,<4
psycopg[binary]>=3.1,<4
